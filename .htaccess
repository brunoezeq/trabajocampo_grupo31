<IfModule mod_rewrite.c>
    RewriteEngine On
    
    # Redirigir cualquier solicitud que NO empiece con public/ hacia public/
    RewriteCond %{REQUEST_URI} !^/trabajocampo_grupo31/public/
    RewriteRule ^(.*)$ public/$1 [L]
</IfModule>